{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="text-center">
        <button class="btn btn-success">
            {{ button_label|default('Dodaj vozilo') }}
        </button>
    </div>
{{ form_end(form) }}
